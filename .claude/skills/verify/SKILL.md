# Verify static site

Run `python -m http.server 8000` from the workspace root and request the main HTML pages plus shared CSS/JS at `http://127.0.0.1:8000/`. For full UI verification, drive each route in a browser and check theme persistence, dropdown dismissal, filters, action-count toggles, copy feedback, and mobile navigation.
